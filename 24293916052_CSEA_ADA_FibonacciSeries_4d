#include <stdio.h>

int fib_bottomup(int n) {
    int dp[n + 1];
    dp[0] = 0;
    dp[1] = 1;
    for (int i = 2; i <= n; i++)
        dp[i] = dp[i - 1] + dp[i - 2];
    return dp[n];
}

int main() {
    int n = 10;
    for (int i = 0; i < n; i++)
        printf("%d ", fib_bottomup(i));
    return 0;
}
